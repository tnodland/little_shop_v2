<main class="merchants-items-index">
  <%= link_to "Add Item", new_dashboard_item_path %>
  <section class="data-table shadowed-box">
    <% @items.each do |item|%>
      <%= render 'shared/merchant_item', item:item %>
    <% end %>
  </section>
</main>
