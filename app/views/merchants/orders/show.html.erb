<% @items.each do |item| %>
  <section id="ordered-item-<%= item.id %>">
    <p><%= link_to "#{item.name}", item_path(item) %></p>
    <p>Price: <%= number_to_currency(item.current_price) %></p>
    <p>Quantity ordered: <%= item.amount_ordered(@order) %></p>
  </section>
<% end %>
