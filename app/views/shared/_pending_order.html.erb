<div id=<%="order-#{order.id}"%>>
  <%= link_to "Order #{order.id}", dashboard_order_path(order) %>
  Created: <%= order.created_at %>
  Cost: <%= order.total_cost %>
  Items: <%= order.total_count %>
</div>
